# Tunel HTTPS → Spring Boot (:8080). Solo API (/api/v1), nunca la UI.
# La app vive en https://frontend-beta-ten-40.vercel.app

version: "2"
tunnels:
  backend:
    addr: 8080
    proto: http
    domain: blade-jot-uncommon.ngrok-free.dev
